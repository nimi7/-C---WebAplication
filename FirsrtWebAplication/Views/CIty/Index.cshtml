@model IEnumerable<FirsrtWebAplication.Models.City>

@*@Html.AntiForgeryToken()
@if (Session["FullName"].ToString() == "Nimrod Wandam")
{
    ViewBag.Title = "SeeUser";
    Layout = "~/Views/Shared/ccc.cshtml";
}
else
{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}*@
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Index</h2>

<div>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    @using (Html.BeginForm("Index", "City", FormMethod.Get))
    {
        <p>
            FirstName : @Html.TextBox("Search") <br />
            <input type="submit" value="Search" />
        </p>

    }
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CityName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CityName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Cityid }) |
                @Html.ActionLink("Details", "Details", new { id = item.Cityid }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Cityid })
            </td>
        </tr>
    }

</table>
